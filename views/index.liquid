<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    {% layout "layout.liquid" %}
    {% block content %}
    <div class="container">
        {% for player in players %}
        <div class="items">
        <h3>{{player.name}}</h3>
        <a href="/players/{{player._id}}"><img src={{player.img}}></a>
        </div>
        {% endfor %}
    </div>
    {% endblock %}
</body>
</html>